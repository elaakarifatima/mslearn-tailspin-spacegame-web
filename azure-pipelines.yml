trigger:
- main

pool:
  vmImage: 'Default'
  # ou bien :
  # name: MyAgentPool  # si tu veux spécifier ton propre agent pool

variables:
  buildConfiguration: 'Release'

steps:
- bash: echo "hello"

- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    projects: '**/*.csproj'
    arguments: '--configuration $(buildConfiguration)'
    displayName: 'Build .NET project'
